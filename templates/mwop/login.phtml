<?php $this->layout('layout::layout', [
    'title'  => 'Login',
    'layout' => 'small',
]) ?>
<section class="flex flex-row justify-items-center">
    <div class="bg-mwop-nav p-4 w-full rounded-lg">
        <h2>Login</h2>

        <?php if (isset($error)) : ?>
        <p class="my-4 p-2 bg-red-700 text-white font-bold">
            Unable to validate your credentials. Please try again.
        </p>
        <?php endif ?>

        <form method="post" name="login" action="<?= $this->url('login') ?>" id="login">
            <label class="label" for="username">Username:</label>
            <input class="text-input" type="text" name="username" value="" placeholder="Enter your username" required="required">

            <label class="label" for="password">Subject:</label>
            <input class="text-input" type="password" name="password" value="" placeholder="Provide your password" required="required">

            <button class="btn" type="submit" name="login">Login</button>
        </form>
    </div>
</section>
